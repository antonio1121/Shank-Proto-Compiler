public class interpreter {

    public float resolve(node node) {

       // if(node.getClass().isAssignableFrom(floatNode.class)) {
         //   return floatNode ;
        return 0 ;
        }
    }

